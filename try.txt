git is a version control system.
I have to learn git.
today is 20151125,it is second time learn git command.
git status
git checkout filename ----giveup file changes in working directory
i want learn command diff
now i am learning ,不行了英语不会了，我是在学git的工作区，临时工作区，版本库
回退操作
回退的操作2
回退操作：没有进行add操作，工作区的没有提交到临时工作区
我在进行冲突操作
我在进行冲bug分支操作，这个非常有用的操作
我在用git checkout -b dev 创建分支之后，修改了try.txt文件，但是没有进行add和commit操作，直接切换到了master分支，发现try.txt是修改的，就是在Dev分支上做的修改。理解创建分支是增加了指针。下面我想做一次尝试，在dev上修改之后，add操作之后是什么样的。

